package org.chardev.cjt;

public class DBCParserLocalization {
	// create a parser for every dbc file, write them into the mysql db and finally join within the database
	// update $tableName set $column_i = ( select $column_i from  )
}
